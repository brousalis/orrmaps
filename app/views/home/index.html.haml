= render :partial => 'layouts/secondary', :locals => {:map => @map} if current_user
#main{:role => "main"} 
  #map
:javascript
  orrmaps.index.init();
- if !session[:alert3]
  :javascript
    orrmaps.alert.init("Server nodes reset on <strong>9/19</strong>", 8000);
    orrmaps.alert.init("Southsun Cove has been added! I'm very sorry, but I had to reset all marker locations to use the new maps. Several bugs were also fixed.", 10000);
